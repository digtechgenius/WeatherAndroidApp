name: Android Pull Request & Master CI & CD to Firebase

on:
  pull_request:
    branches:
      - 'master'
  push:
    branches:
      - 'master'

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Unit tests
        run: bash ./gradlew test --stacktrace

  build:
    name: Upload apk to App Tester
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: build release 
      run: bash ./gradlew assembleDebug
    - name: upload artifact to Firebase App Distribution
      uses: digtechgenius/WeatherApp@master
      with:
        appId: '1:1043505561377:android:1327d4fdeba1a64a318860'
        token: '1//03rGbxus4E6GcCgYIARAAGAMSNwF-L9Ir7pZUV6znQn7yKkQouYiry4i_Hs34117OfLiddA42XdvRU1F52cHv8aY--dnHvVnYMLI'
        groups: Demo
        file: app/build/outputs/apk/debug/app-debug.apk

  apk:
    name: Generate APK
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Build debug APK
        run: bash ./gradlew assembleDebug --stacktrace
      - name: Upload APK
        uses: actions/upload-artifact@v1
        with:
          name: app
          path: app/build/outputs/apk/debug/app-debug.apk
